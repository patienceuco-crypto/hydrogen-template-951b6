backend:
  name: git-gateway
  branch: main

site_url: https://sparkling-tanuki-95f8d2.netlify.app

media_folder: "Static/Images"
public_folder: "/Static/Images"

collections:
  - name: "products"
    label: "Products"
    folder: "content/products"
    create: true
    extension: "json"
    format: "json"
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Price (NGN)", name: "price", widget: "string" }
      - { label: "Category", name: "cat", widget: "select", options: ["Wigs","Shoes","Bags"] }
      - { label: "Image", name: "img", widget: "image" }
